/**
 * Intentionally no hosted database.
 *
 * NASCAR 25 Setup Lab persists owner-entered records in IndexedDB on the device.
 * This module remains only because the Sites starter reserves the directory.
 */
export const hostedDatabase = null;
